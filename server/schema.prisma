datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

generator client {
    provider = "prisma-client-js"
}

model Oauthuser {
    user_id       String    @id
    name    String
    email String
    latitude Int
    longitude Int
}
